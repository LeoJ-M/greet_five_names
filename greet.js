// create a function called greet that
// takes a name parameter
// log a greeting to that name in the fucntion


function greet(name) {
    console.log(`Hello, ${name}.`);
}

// console.log(names);
module.exports = greet;